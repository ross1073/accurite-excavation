# 2026-08-11 — monthly report launchd job has never worked; July report built by hand + 6 fixes shipped

## What we did
- Diagnosed why the `com.rosswalker.accurite-monthly-seo-report` launchd job has never once succeeded (broken since its first scheduled run in June), then generated the July 2026 report by hand end-to-end: GSC pull, lead re-tag, fresh GBP public-data pull, PDF archived, Teamwork review task filed.
- Shipped 6 commits in `~/projects/3sm_code` (73d6b0f..3edbb7c, pushed to origin/main): moved report Teamwork tasks to the correct project, dropped the flag that was killing every scheduled run, hid the empty GBP section, then replaced it with a real data-driven GBP section (`pull_gbp_reviews.py`, 19 tests passing).
- Moved TW task 36927297 into project 210055 "Shawn Durrant" / tasklist 1694372 "Inbox" via v1 REST (v3 doesn't support project moves) and corrected an earlier wrong claim about where the June report task lives (3edbb7c).

## Decisions made
- Suppress the clicks metric (-23.4%) on the July PDF only, per Ross's explicit "yes, just this time" — not committed anywhere; August prints clicks again unless decided otherwise.
- GBP section of the report is now fed from `data.json` + a public DataForSEO pull instead of staying a stub behind the unapproved GBP API — this is a feature (4e4709f), not a workaround.
- July report's GBP-posting bullet deliberately carries no post count, so it can't imply weekly cadence during a month with a known posting gap.

## July 2026 report — the numbers as shipped
- Leads 4 (June 6). Trailing 6mo: Feb 0, Mar 2, Apr 4, May 6, Jun 6, Jul 4. The down-MoM auto-hid; the sparkline carries it.
- Clicks 98 vs 128 (−23.4%) — **row suppressed in the PDF, this month only**. Impressions 19,987 (+8.6%). Queries ranked 1,095 (+20.3%).
- Top movers: `land clearing contractor graphite` 7.8→4.0, `excavation services near me` 8.0→4.4, `excavation companies near me` 5.8→4.1. Homepage top page at 38 clicks.
- GBP section: 61 reviews / 4.9★ / +1 this month.
- Inputs were `/tmp/accurite-report-2026-07.json` — **/tmp is ephemeral**; regenerate from `ROUTINE.md` rather than expecting it to survive.

## Open threads / next steps
- ~~July report awaiting review~~ — **CLOSED 2026-08-11.** Ross handled it; TW 36927297 marked complete.
- ~~Did Ross post to GBP manually during the 06-02→07-21 gap?~~ — **ANSWERED 2026-08-11: yes, by hand.** So the report's "posted updates with job photos through the month" bullet is accurate and understates the work. **Consequence for future reports: `.claude/skills/gbp-post/post-log.md` records only what the automated skill generated — Ross also posts manually, so the log is a floor, never the true post count. Never cite it as the number of posts in a month.**
- 2026-09-01 08:00 launchd run is the real test of the `--channels` fix — could not be verified end-to-end this session (Claude Code can't nest inside itself to run the routine live).
- May 2026 report was never generated at all (job broken since inception) — decide whether to backfill.
- Lead count discrepancy: the already-shipped June report says 5 leads; after today's re-tag, internal data shows 6 (Mike Andrus, 06/24, newly tagged). The two documents now disagree — unresolved, not corrected retroactively.
- Ask Ross whether he posted GBP updates manually during the 06-02→07-21 gap — post log shows only 2 GBP posts in all of July (07-21, 07-28).
- TW 36440380 (April, completed) and TW 36440438 (GBP-API wiring, now superseded by 4e4709f) are still sitting in the old R&R tasklist 2518534.
- Two GSC top-mover queries carry nonsense geography ("graphite", "elnora") straight from Search Console — flagged on the task, unresolved.
- Gmail and Google Calendar MCP connectors need reauthorization via claude.ai connector settings.
- Teamwork MCP OAuth is expired again — this session's Teamwork work went entirely through REST (curl + `TEAMWORK_API_TOKEN`, and `~/.local/bin/tw`).

## Gotchas surfaced
- `run-monthly.sh` passed `--channels plugin:telegram@claude-plugins-official` to the Claude Code CLI — that flag does not exist in v2.1.70. Every scheduled run (2026-06-01, 07-01, 08-01) died on `error: unknown option '--channels'` before ever reading ROUTINE.md. Log: `~/Library/Logs/accurite-monthly-report.log` (only 3 entries, all identical).
- The lead domain-tagging drift (previously "fixed" by hand on 2026-07-03) recurred immediately — July's initial parse showed only 2 leads (both 07/02, nothing after) until Ross manually re-tagged mid-session, landing at 4.
- Teamwork v3 API refuses cross-project task moves ("not supported yet by this service"); v1 `PUT /tasks/{id}.json` with `{"todo-item":{"todo-list-id":"..."}}` works.
- No Teamwork project named "AccuRite" or containing "excavation" exists — confirmed via a 0-result search across all statuses. AccuRite tasks live under "Shawn Durrant" (project 210055).
- `~/projects/3sm_code` has an auto-sweeper that commits working-tree drift, so the one-off clicks suppression had to be edited into the template, rendered, and reverted inside a single shell command, then verified the tree was back to baseline.

## Files of note
- `~/Library/Logs/accurite-monthly-report.log` — the 3-line proof the launchd job has never succeeded.
- `~/projects/3sm_code/monthly-reports/accurite/ROUTINE.md` and `README.md` — now point at Teamwork project 210055 / tasklist 1694372 (was 628283 / 2518534); ROUTINE also gained steps 3b/3c for the GBP block.
- `~/projects/3sm_code/monthly-reports/accurite/pull_gbp_reviews.py` (new, commit 4e4709f) — public-profile DataForSEO pull feeding the report's GBP section, no GBP login needed. Run: `DATAFORSEO_AUTH=... python3 pull_gbp_reviews.py --prior-count 61`.
- `~/projects/3sm_code/monthly-reports/accurite/run-monthly.sh` — the launchd entry point; `--channels` removed here (6d71521).
- `~/projects/accurite-excavation/research/local-pack/review-velocity-2026-08-11.json` — fresh GBP pull: AccuRite 61 reviews/4.9★/rank 2 (+1 review since 2026-06-18 baseline of 60); Skinner Excavating rank 1 with 9 reviews, stale since 2023-10-26.
- `~/Documents/accurite-reports/accurite-seo-2026-07.pdf` — the hand-built July report delivered to the client task.
- Teamwork task 36927297 "Review SEO report — July 2026" — now correctly filed under 210055/Inbox, due 2026-08-14.

---

## Session 2026-08-11T20:28:20Z

### Decisions
- Suppress clicks metric (−23.4%) from July 2026 PDF — Ross explicit "yes, just this time". August report prints it again unless re-decided.
- GBP section now fed by `pull_gbp_reviews.py` (public DataForSEO pull) rather than a stub behind the unapproved GBP API; permanent feature (commit 4e4709f in 3sm_code).
- July report GBP-posting bullet carries no post count, to avoid implying weekly cadence during the known 06-02→07-21 posting gap.
- Teamwork v1 REST (`PUT /tasks/{id}.json`) is the only way to move tasks cross-project; v3 refuses it. Retain this pattern.

### Work completed / shipped
- `6cf7e13` (this repo) — daily note for 2026-08-11 session (written by /wrap).
- `9cd920a` (this repo) — GBP Service Highlight post logged (TW 36926145).
- 6 commits in `~/projects/3sm_code` (73d6b0f..3edbb7c, pushed origin/main):
  - Teamwork pointers corrected: project 210055 / tasklist 1694372 (was 628283 / 2518534).
  - `--channels` flag removed from `run-monthly.sh` (killed every scheduled run since June).
  - Empty GBP stub hidden, then replaced by real data-driven block (`pull_gbp_reviews.py`, 19 tests passing).
  - TW task 36927297 moved to 210055/Inbox via v1 REST.
- July 2026 report built by hand end-to-end: GSC pull, lead re-tag (4 leads), DataForSEO GBP pull (61 reviews/4.9★), PDF rendered and archived to `~/Documents/accurite-reports/accurite-seo-2026-07.pdf`.

### Items closed
- **Tuesday 2026-07-21 GBP launchd first-run verification** — CLOSED. GBP post commits 07-21, 07-28, 08-04, 08-11 confirm `com.rosswalker.accurite-gbp-weekly` is running on schedule. SUPERSEDES open item in 2026-07-17.md § Session 2026-07-17T19:34:19Z.
- **Monthly report `--channels` breakage** — root-cause confirmed and fixed in 3sm_code (6d71521). The 2026-09-01 run is the first live automated test.

### Items changed / deferred / blocked
- **Monthly automation end-to-end test** — deferred to 2026-09-01 08:00 launchd run; Claude Code can't self-invoke to test headless.
- **May 2026 report backfill** — decision deferred; Ross to decide.

### New open items
- **TW 36927297** — "Review SEO report — July 2026", due 2026-08-14, project 210055/Inbox. Nothing has gone to the client yet; awaiting Ross's review.
- **Lead count discrepancy** — June report (already delivered) says 5 leads; re-tag shows 6 (Mike Andrus 06/24 newly tagged). Two documents now disagree; not corrected retroactively.
- **GBP posting gap** — ask Ross if he posted GBP updates manually during 06-02→07-21; the log shows only 07-21 and 07-28 in all of July.
- **Stale TW tasks in wrong tasklist** — TW 36440380 (April, completed) and TW 36440438 (GBP-API wiring, superseded) still sit in old R&R tasklist 2518534; clean up when convenient.
- **GSC nonsense geography** — top-mover queries include "graphite" and "elnora" with no clear local relevance; flagged on the TW task, unresolved.
- **Gmail + Google Calendar MCP** — need reauthorization via claude.ai connector settings.
- **Teamwork MCP OAuth** — expired again; all Teamwork work this session went through REST. Reauth or leave as REST-only.
- **Leftover disabled cloud triggers** (from 07-17) — delete via claude.ai routines UI; no urgency.
