# 2026-06-23 — Fixed blog-index .md 404s (link fix + 13 per-slug 301s), diagnosed 2 posts invisible to Google, tasked Cassandra to request indexing

## What we did
- Fixed blog/index.astro: Astro 5 `post.id` includes `.md` extension, so every card link resolved to `/blog/<slug>.md` (404). Stripped `.md` via `.replace(/\.md$/, '')` on both the title and "Read more" links (commit 63a48c3). All 13 posts return 200 live.
- Added 13 enumerated per-slug 301 redirects in netlify.toml for stale `.md` URLs Google may have crawled. Two failed wildcard attempts first (`/blog/*.md` non-terminal splat unsupported; `/blog/:slug.md` placeholder before literal suffix won't substitute). Enumerated the full set instead — commits bcf42f3, 71f4b9c, 6104954. Each `.md` URL → 301 → clean URL → 200 verified live.
- GSC URL Inspection on all 13 posts: 8 indexed, 5 not. `excavation-safety-on-federal-projects` unknown to Google (only internal link was the broken .md one); `national-park-service-project` discovered but not crawled. Three others (avoid-flooding-new-home, retaining-wall-permit-utah, retaining-wall-types-utah-soil) crawled but not indexed — quality decision by Google, not a discovery gap. Created Teamwork task 36664201 for Cassandra (Shawn Durrant project 210055, due 2026-06-26) to manually Request Indexing for the 2 discovery-blocked posts.

## Decisions made
- Only the 2 discovery-blocked posts go to Cassandra for Request Indexing; the 3 "crawled, not indexed" posts need content improvement, not resubmission — excluded deliberately.
- Enumerated 13 per-slug redirects rather than a wildcard because Netlify can't strip a suffix with a placeholder or non-terminal splat. The fixed index emits no new `.md` URLs so this set is complete and closed.

## Open threads / next steps
- Cassandra to complete Request Indexing for `excavation-safety-on-federal-projects` and `national-park-service-project` in GSC by Fri 2026-06-26 (TW 36664201: https://csfund.teamwork.com/app/tasks/36664201).
- The 3 "crawled, currently not indexed" posts are a future content-quality effort if Ross wants them indexed — not yet scoped.
- GSC OAuth token (command-center google_oauth_creds.json) is READ-ONLY (webmasters.readonly) — sitemap resubmit or any GSC write returns 403. Re-consent with full webmasters scope requires Ross's browser (OAuth UI action, can't be done programmatically).

## Gotchas surfaced
- Astro 5 `post.id` includes the file extension (`.md`). The `[...slug].astro` route already compensated; `blog/index.astro` didn't. Any future code linking from `post.id` must strip `.md`.
- Netlify redirect rules: splat must be terminal (`/blog/*.md` silently doesn't match); a `:placeholder` before a literal suffix won't substitute (emits literal `:slug`). Enumerate when stripping a file extension suffix.
- "Request Indexing" has no GSC API — it's UI-only. The read-only token wouldn't have helped even if we'd had write scope.

## Files of note
- `src/pages/blog/index.astro` — .md strip fix on both post card links
- `netlify.toml` — 13 per-slug 301 redirects for stale .md URLs Google may have cached
