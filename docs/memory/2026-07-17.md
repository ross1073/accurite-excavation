# 2026-07-17 — fixed weekly GBP-post automation by moving it off the broken claude.ai cloud routine to local launchd

## What we did
- Diagnosed why the weekly GBP-post task had produced nothing since 2026-06-16 (07-14 run and a manual test both silent): the claude.ai RemoteTrigger cloud sandbox blocks outbound HTTPS to csfund.teamwork.com (proxy 403 connect_rejected) — REST client and TEAMWORK_API_TOKEN were fine, the sandbox network egress was the actual wall. The 07-13 "REST fix" (docs/memory reference note) had only moved the failure from expired-MCP-OAuth to network-blocked-REST and was never actually tested from the cloud.
- Migrated the routine to a local macOS launchd job: `com.rosswalker.accurite-gbp-weekly.plist` → `scripts/run-gbp-weekly.sh` → runs the gbp-post skill headless with the REST client, Tuesday 12:03 PM MDT (runs on next wake if the Mac is asleep). Loaded and confirmed scheduled. Disabled the old cloud routine (trig_01UDgFrnCR1Vb6arm1Wr76cX).
- Verified the REST client reaches Teamwork from a launchd-like minimal env (real data returned, exit 0); committed and pushed the runner + skill-doc update (c258b73, confirmed on origin/main); corrected the memory note `reference_gbp_weekly_routine.md` that had misled this session's early diagnosis.

## Decisions made
- Run the weekly GBP post locally off Ross's Mac via launchd, not GitHub Actions — his machine is usually on Tuesday afternoons.
- Keep the Teamwork REST client, never MCP — MCP's expiring OAuth caused the original 3-week silent outage.

## Open threads / next steps
- Tuesday 2026-07-21 12:03 PM is the first live launchd run — confirm a task actually lands in Teamwork afterward. The runner calls `~/.local/bin/notify` on failure.
- Could not run a full nested end-to-end test this session (Claude Code refuses to launch nested inside another Claude session via the CLAUDECODE env guard); plumbing was verified piecewise instead.
- Leftover disabled one-shot diagnostic triggers exist in the claude.ai cloud, deletable only via the routines UI.
- Prior open items from 07-14 still stand: client-facts verification of the 11 About-page orgs, sewer data cross-check, trenchless vs open-cut decision, GSC OAuth, GBP Performance API, 3 crawled-not-indexed posts.

## Gotchas surfaced
- claude.ai RemoteTrigger cloud sandboxes block outbound HTTP to almost every host (only Google APIs allowed) — any scheduled task hitting an external API (Teamwork, etc.) must run locally (launchd/cron) or on GitHub Actions, not a cloud routine.
- Proposed three wrong root causes (missing token, switch to MCP, an invented claude.ai "Custom network allowlist" UI feature that doesn't exist) before a second head check surfaced the real cause. Failure mode: over-trusting a single framing and inventing an unverified fix mechanism instead of testing.
- Auto Mode's classifier blocks creating/updating a routine that writes to Teamwork under Ross's identity, and blocks `launchctl bootstrap` (installing a LaunchAgent) — both required manual Shift+Tab approval.
- Claude Code cannot be launched nested inside another Claude Code session (CLAUDECODE env guard) — blocks self-testing headless runs from within a session.

## Files of note
- `scripts/run-gbp-weekly.sh` — new launchd runner (headless claude → gbp-post skill, REST-only)
- `~/Library/LaunchAgents/com.rosswalker.accurite-gbp-weekly.plist` — weekly schedule config (machine-local, not in repo)
- `.claude/skills/gbp-post/SKILL.md` — Scheduling section updated to launchd + rationale
</content>
