# 2026-05-28 — Post-core-update SEO re-check + GBP category overhaul + new /services/land-clearing page

## What we did
- Reconstructed and ran DataForSEO local-pack + GSC re-pull (originals were /tmp casualties); compared against the 2026-05-22 baseline. Map Pack steady (Ogden #1, Pleasant View #3, Layton improved #4→#3). "Excavation company" organic position recovering toward #1 but impression counts are too thin to call decisively. Scripts now live in `research/local-pack/` so they survive next /tmp cleanup.
- Audited AccuRite's GBP categories via DataForSEO Maps endpoint, walked Ross through Google's picker (which is restrictive for primary=Excavating contractor). Final state Shawn applied: 1 primary + 6 additional — Excavating contractor / Septic system service / Drainage service / Trucking company / Earth works company / Demolition contractor / Retaining wall supplier. Picker is now maxed for this profile.
- Shipped focused `/services/land-clearing` page (option C: focused sub-page coexisting with the combined `/services/grading-land-clearing` hub, no redirect) to target the "land clearing ogden" Map Pack gap query that categories can't solve. Pushed to main, verified live at https://accuriteexcavation.com/services/land-clearing (200, correct title).

## Decisions made
- Keep Septic system service GBP category. Earlier-in-session recommendation to drop it was wrong — `src/content/services/septic-systems.md` is a real, detailed page; AccuRite genuinely does septic work in Weber/Davis/Morgan.
- Split strategy = option C (additive). New `/services/land-clearing` lives alongside the combined `/services/grading-land-clearing` page; no 301 redirect, both URLs coexist. Lowest-risk way to target the gap query without disturbing existing rankings.
- Category-side leverage is tapped out. Picker refuses "Site preparation contractor", "Land clearing service", "Grading contractor" for primary=Excavating contractor. Remaining moves for those gap queries are on-site content + GBP Services list + reviews + geotagged photos — not categories.
- Mid-update SEO check is informational only. May 2026 Google core update started 2026-05-21 and hasn't been declared complete; treating 05-28 numbers as a checkpoint, not a verdict.

## Open threads / next steps
- Re-check `/services/land-clearing` indexation and "land clearing ogden" impressions in GSC in 10-14 days. If page picks up impressions, that validates the split-out strategy and we'd consider a parallel `/services/grading` split.
- Re-pull local-pack + GSC for "excavation company" once Google announces core update rollout complete (~early June). Scripts ready at `research/local-pack/pull_localpack.py` and `pull_gsc_excavation_company.py`.
- Project-brief and CLAUDE.md referenced a `project_accurite_seo_state.md` memory file that doesn't exist — surfaced this session but already addressed by commit f37aca9 (repointed refs to actual memory/seo docs).
- Open build decision still hanging from 05-22: GBP Performance API collector (calls/directions/impressions) needs OAuth scope expansion + GBP location ID + a command-center collector. Not started.

## Gotchas surfaced
- DataForSEO's category list (5,277 entries) is much broader than Google's GBP picker actually offers for any given primary category. The picker filters down hard — "Site preparation contractor" exists in DataForSEO's dump but is not selectable on a profile with Excavating contractor as primary. Don't promise category names from DataForSEO without acknowledging picker may reject.
- My early-session "no septic service page exists" claim was wrong. The file exists and is detailed. Lesson: before recommending dropping a category that names a service, grep for that service in `src/content/services/` instead of asserting from memory or partial reads. Logged as feedback memory.
- Local-pack DataForSEO parser bug: each pack member returns as its own top-level item with `type: "local_pack"`, not nested inside one wrapper item. My first parse returned pack_size=1 for every query. Fixed; correct shape now documented in the script.

## Session 2026-05-28T18:59Z — Post-core-update re-check + GBP categories + /services/land-clearing

*(Detail captured inline above by /wrap. This block is the memory-keeper index entry.)*

### Decisions
- Keep Septic system service GBP category (live service page exists, was wrongly flagged for removal).
- New `/services/land-clearing` page: Option C (additive, coexists with combined grading-land-clearing hub, no redirect).
- GBP category picker maxed; no further adds available for primary=Excavating contractor.
- Mid-update numbers (05-28) are a checkpoint only — core update not declared complete.

### Work completed / shipped
- Commits: `18bc83c` (land-clearing page + research scripts), `0d2bca2` (docs + brief + stage-current refresh), `f37aca9` (repoint phantom seo_state refs).
- `/services/land-clearing` live and returning 200 at production URL.
- DataForSEO local-pack + GSC pull scripts moved to `research/local-pack/` (permanent home).
- GBP category set updated by Shawn: 1 primary + 6 additional (Earth works company, Drainage service, Trucking company added; picker now maxed).
- 2 new global memory files: `feedback_verify_before_removing.md`, `reference_dataforseo_gbp_categories.md`.

### Items closed
- `/tmp` script loss (scripts now in repo).
- Phantom `project_accurite_seo_state.md` reference — brief/CLAUDE.md repointed to real docs.

### Items changed / deferred / blocked
- GBP category leverage: tapped out (no further work possible here).
- May 2026 core update verdict: deferred to ~early June re-check.

### New open items
- Re-check `/services/land-clearing` GSC impressions + "land clearing ogden" pack in 10-14 days.
- Re-pull local-pack + GSC once core update rollout is declared complete.
- GBP Performance API collector (calls/directions/impressions) still unstarted — needs OAuth + location ID + command-center collector.

## Files of note
- `src/content/services/land-clearing.md` — new focused service page (731 words + FAQs), targets "land clearing ogden" gap query.
- `src/data/services.json` — new entry for land-clearing slug.
- `src/pages/services/[...slug].astro` — heroImageMap + serviceGalleryMap extended for new slug (reuses grading hero + relevant gallery photos).
- `research/local-pack/pull_localpack.py` — DataForSEO 3-pack puller for 3 geo points × 3 queries; persistent home for the /tmp casualty.
- `research/local-pack/pull_gsc_excavation_company.py` — GSC daily-position puller with pre/mid/post window aggregation.
- `research/local-pack/results-2026-05-28.json` — 05-28 local-pack snapshot for future delta comparisons.
- `research/local-pack/gbp-categories-full.json` — full DataForSEO Google categories list (5,277 entries) for offline category exploration.
- `~/.claude/projects/-Users-rosswalker-projects-accurite-excavation/memory/reference_accurite_gbp_categories.md` — new memory recording the post-05-28 GBP category set + stable place_id/cid + "don't propose more adds" constraint.
