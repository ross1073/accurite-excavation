# 2026-05-22 — AccuRite homepage title/meta CTR test review + Map Pack rank analysis

## What we did
- Reviewed the 2026-04-30 homepage title/meta change (PR #2) against GSC: the CTR test is unmeasurable (~0-1 clicks/window on the three target queries); a weekly breakdown showed the "drastic position drop" was largely a bucketing artifact, except "excavation company" which genuinely slid from organic ~1.5 to #13 starting ~2026-05-11.
- Pulled DataForSEO local-pack (Map Pack) positions from three geo-points — AccuRite is #1 in the 3-pack for "excavation companies near me" from Ogden, decaying to #3 (Pleasant View) and #4 (Layton), and absent from the pack for "land clearing ogden" and "grading contractor near me" everywhere.
- Brought in the local-SEO advisor for Map Pack improvement advice and created two Teamwork tasks in the "Shawn Durrant" project (Inbox tasklist): one for the GBP category/review action plan, one for post-core-update follow-up checks.

## Decisions made
- Keep the new homepage title/meta — do not revert. The organic slide is in the system that matters least for local intent (Map Pack #2 holds), and reverting mid-core-update would poison the diagnosis.
- Measure Map Pack position via DataForSEO, not GSC, for local-intent queries; GSC organic position is the wrong scoreboard for a local service business.
- File AccuRite tasks under the "Shawn Durrant" Teamwork project (210055), confirmed correct by Ross.

## Open threads / next steps
- After the May 2026 Google core update finishes (~early June; started 2026-05-21): re-pull "excavation company" GSC to see if it recovers, investigate what changed ~May 11, and re-run the local-pack scripts.
- Decide whether to build a GBP Performance API collector (calls/directions/impressions) — needs new OAuth scope (business.manage) + GBP location ID + a command-center collector.
- Content gap: dedicated land-clearing and grading service pages (also in project_accurite_seo_state.md).
- GBP category + review-workflow actions require Shawn (client owns the profile).

## Gotchas surfaced
- GSC "average position" on tiny impression counts (single digits/week) is noise; comparing a 90-day baseline against a 30-day window straddling the change date produced a misleading "drop" — equal-length windows + weekly breakdown were needed to see the truth.
- The GBP Performance API cannot return Map Pack rank position — only impressions and actions. Rank position is a DataForSEO job.

## Files of note
- `/tmp/accurite_localpack.py`, `/tmp/accurite_localpack_geo.py` — one-off DataForSEO local-pack pull scripts (Ogden / Pleasant View + Layton); reusable for the follow-up re-check.
- `~/.claude/projects/-Users-rosswalker-projects-accurite-excavation/memory/reference_accurite_teamwork_project.md` — new memory: AccuRite tasks live in the "Shawn Durrant" project.
- `~/projects/3sm_code/command-center/collectors/dataforseo.py` / `gsc.py` — existing collectors used as the basis for the one-off pulls (cross-project: command-center).

---

## Session 2026-05-22T15:48:09Z

### Decisions
- Memory-system plumbing overhaul approved: fix `/wrap` Step 5 to do full-tree reconciliation (not just `docs/` + `.claude/` blanket stage), add auto-commit safety net in SessionEnd hook, loosen memory-keeper skip logic that was producing false "already documented" exits.
- Gitignore `~/.claude/` log file (Claude session log) to stop it showing up as an untracked nuisance in `git status`.

### Work completed / shipped
- **`ef055da`** `chore(memory): convert SessionEnd hook to logged command-mode with recursion guard` — cleaned up the hook's execution model.
- **`a79a5ae`** `docs: commit accumulated brief refresh, stage doc, daily note, plans + IndexNow key` — resolved all leftover uncommitted docs that had accumulated across prior sessions (project-brief.md, stage-current.md, the IndexNow key file, wrap/skills/agents files).
- **`b217599`** `fix(memory): notes-only auto-commit + stop memory-keeper false-skipping` — three-part fix:
  1. `/wrap` Step 5 (`~/.claude/commands/wrap.md`, global): replaced blanket `docs/` + `.claude/` staging with full-tree scan that buckets every uncommitted file and commits each group with a descriptive message.
  2. SessionEnd hook: added notes-only auto-commit safety net — if the session ends without `/wrap`, any new `docs/memory/` files get auto-committed so notes don't get stranded again.
  3. Memory-keeper agent (`.claude/agents/memory-keeper.md`): loosened skip logic — removed the false "already documented" exit path that caused the agent to silently skip real sessions when prior-day overlap existed.

### Items closed
- Backlog of uncommitted files that had built up across multiple prior sessions — now committed and pushed. Working tree is clean.

### Items changed / deferred / blocked
- `.claude/agents/memory-keeper.md` and `.claude/hooks/session-end-memory-keeper.sh` were deleted from the repo (shown in `git status -D`); the memory-keeper is now invoked as a subagent from the SessionEnd hook rather than as a standalone agent file checked into the repo. This is intentional — the agent definition lives in `~/.claude/agents/` (global), not in the project repo.

### New open items
- Ross asked for a technical description of the problem and solution to take back to his Claude memory-system project — provided in session but not persisted to a file. If he needs it again: the root cause was `/wrap`'s Step 5 only staged specific subdirs (missing anything outside `docs/` and `.claude/`), and the memory-keeper's skip condition fired on same-day overlap even for new work.
- The `/wrap` fix lives in `~/.claude/commands/wrap.md` (global, outside this repo) — any regression there won't be visible via `git log` in this project.

## Session 2026-05-22T17:30:23Z

### Decisions
- None made. Pure Q&A session.

### Work completed / shipped
- None. No commits.

### Items closed
- None.

### Items changed / deferred / blocked
- None.

### New open items
- **Open question (not yet decided):** Whether to `git init` the `~/projects/mva-tools` and `~/projects/rank-and-rent` container directories so the SessionEnd auto-commit safety net works in those contexts. Analysis: option 1 (leave ungit'd) is safe — the hook fails silently with no corruption, no crash — but the auto-commit/push net becomes a no-op, so notes accumulate as uncommitted local files rather than being pushed. If Ross doesn't care about auto-committing notes in those containers, option 1 is fine as-is.

### Technical analysis surfaced this session
- `session-end-memory-keeper.sh` has no git-repo guard. In a non-git dir it enters the `if` block (due to `!` + non-zero exit from `git diff`), but `git add` and `git commit` fail silently (stderr → `/dev/null`), and the script reaches `exit 0` normally. Memory-keeper agent still runs and writes the daily note; only the auto-commit/push is lost.
- Git's own walk-up behavior is the real hazard: if a `.git` exists above the container, git commands would silently operate on the ancestor repo. Verified: there is no `.git` above `~/projects`, so this hazard doesn't apply to Ross's current setup.
- Verified filesystem: `~/projects/mva-tools` and `~/projects/rank-and-rent` both have their own `.git` today; `~/projects` itself does not.
