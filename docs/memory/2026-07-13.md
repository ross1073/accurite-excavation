# 2026-07-13 — daily note

## Session 2026-07-13T21:00Z (session 1facb2bb — formally closed today)

### Decisions
- None made today; session closed with no new work on 2026-07-13.

### Work completed / shipped
- All actual work in this session was done on 2026-06-23 and 2026-07-03 and is fully documented in those dated notes:
  - `docs/memory/2026-06-23.md` — blog-index `.md` 404 fix (commit `63a48c3`) + 13 per-slug 301 redirects (commits `bcf42f3`, `71f4b9c`, `6104954`) + GSC inspection (2 posts discovery-blocked)
  - `docs/memory/2026-07-03.md` — June 2026 SEO report generated end-to-end; 3 generator fixes in seomachine (commit `5d1aec5`); PDF archived at `~/Documents/accurite-reports/accurite-seo-2026-06.pdf`; TW review task 36729916 created

### Items closed
- Session 1facb2bb formally ended. Both /wrap commands ran at the time, creating the 2026-06-23.md and 2026-07-03.md blocks correctly.

### Items changed / deferred / blocked
- TW 36664201 (Cassandra GSC indexing task, due 2026-06-26): outcome unknown — no follow-up seen this session. Now ~3 weeks overdue.
- TW 36729916 (Ross review of June 2026 PDF, due 2026-07-06): outcome unknown — now ~1 week overdue.

### New open items
- **Verify Cassandra completed TW 36664201**: she was assigned to manually Request Indexing for `excavation-safety-on-federal-projects` and `national-park-service-project` in GSC by 2026-06-26. Confirm or re-assign if missed.
- **June 2026 PDF still unsent to client**: PDF at `~/Documents/accurite-reports/accurite-seo-2026-06.pdf` is ready; Ross reviews via TW 36729916, then forwards to AccuRite.
- **GSC OAuth token is read-only** (webmasters.readonly scope only). Any GSC write (sitemap resubmit, etc.) requires browser re-consent with full webmasters scope — must be done by Ross in browser. Documented in 2026-06-23.md.
- **Teamwork MCP token expired**: connector needs re-auth in claude.ai settings. REST API fallback (TEAMWORK_API_TOKEN as Basic auth username) works fine in the interim.
- **GBP Performance API still pending** Google approval (applied 2026-05-12); report shows a stub until approved.
- **3 "crawled, not indexed" blog posts** (avoid-flooding-new-home, retaining-wall-permit-utah, retaining-wall-types-utah-soil): Google's quality decision, not a discovery gap. Content improvement needed if Ross wants them indexed — not yet scoped.

## Session 2026-07-13T20:10Z

### Decisions
- **REST API for Teamwork everywhere, system-wide.** MCP connector is banned from any unattended cloud routine because it uses OAuth that expires and cannot be re-authed without a human. Static `TEAMWORK_API_TOKEN` via REST is the only acceptable path for automation. Applies to all projects.
- **`tw` CLI (`~/.claude/bin/tw`) is the canonical local Teamwork client.** All projects either call this directly or vendor a copy (`scripts/teamwork.py`) for cloud sandbox use.
- **GBP post-log.md is best-effort only.** Log pushes run after task creation; a failed push can no longer abort the post. Teamwork task is the source of truth for post history.

### Work completed / shipped
- **Root cause found**: GBP weekly post routine (`trig_01UDgFrnCR1Vb6arm1Wr76cX`) was creating Teamwork tasks exclusively via the claude.ai MCP connector. That connector's OAuth token expired ~2026-06-23. Three Tuesdays (06-23, 06-30, 07-07) fired the cron, generated the GBP post, then silently dropped the Teamwork task creation with no error surfaced. Last task received by Teamwork was 2026-06-16.
- **Created `~/.local/bin/tw`** (later moved to `~/.claude/bin/tw`): Python REST client using `TEAMWORK_API_TOKEN` from env or `~/.config/secrets/secrets.env`. Verified working (`whoami` → Ross Walker).
- **Vendored `scripts/teamwork.py`** in accurite-excavation repo: identical client for cloud sandbox use, committed in `d83529d`.
- **Updated `.claude/skills/gbp-post/SKILL.md`**: Teamwork submission is REST-only via `scripts/teamwork.py`; MCP connector step removed; post-log failure now non-fatal.
- **Updated `~/.claude/skills/teamwork-task/SKILL.md`**: REST-first instruction added, MCP tool calls forbidden for task creation.
- **Updated `~/.claude/projects/-Users-rosswalker/memory/reference_teamwork.md`**: Rewrote to declare REST-first as system-wide canonical rule.
- **Updated per-project memory** `reference_gbp_weekly_routine.md`: Reflects REST-only, notes TEAMWORK_API_TOKEN requirement on routine env.
- **Updated `MEMORY.md` index** in per-project memory.
- **Updated `3sm_code/monthly-reports/accurite/ROUTINE.md`**: Teamwork task creation now says use REST via `~/.local/bin/tw`; MCP references removed.
- **Updated `3sm_code/monthly-reports/accurite/run-monthly.sh`**: Teamwork MCP tools removed from tool allowlist; `Bash` remains so `tw` can run.
- **Updated cloud routine prompt** (`trig_01UDgFrnCR1Vb6arm1Wr76cX`): Prompt rewritten to forbid MCP, use `scripts/teamwork.py` instead. MCP connector detach via API returned 200 but ignored empty `mcp_connections` array — Ross removed it manually in the claude.ai GUI.
- **Added routine notifications** (`trig_01UDgFrnCR1Vb6arm1Wr76cX`): email + push enabled so any future silent failure produces a ping.
- **Ross set `TEAMWORK_API_TOKEN` env var** on the routine's cloud environment in the claude.ai GUI (the one action that required GUI access).

### Commits
- `d83529d` (accurite-excavation, pushed) — `fix(teamwork): replace expiring MCP connector with static-token REST client`
- `65eb823` (3sm_code, pushed) — Teamwork MCP removal from monthly-reports allowlist and run script
- `878d750` (~/.claude, **local only, NOT pushed**) — `feat(teamwork): canonical REST client, ban MCP for unattended work`; includes `bin/tw`, `skills/teamwork-task/SKILL.md`, `reference_teamwork.md`, per-project memory updates

### Items closed
- **TW 36664201 (Cassandra GSC indexing)**: Ross confirmed "she did it" — CLOSED.
- **TW 36729916 (June 2026 PDF review / send to client)**: Ross said "that was done" — CLOSED.
- **Teamwork MCP token expired** (open item from prior session): Root-caused and fixed. MCP connector removed from routine; REST-only path confirmed working.

### Items changed / deferred / blocked
- **~/.claude push blocked by Auto Mode**: `878d750` is committed locally but not pushed. Ross needs to approve or run `git push` in the dot-claude repo. Contains `bin/tw`, global Teamwork skill/memory updates. Until pushed, a machine rebuild would lose the `tw` script (though the symlink at `~/.local/bin/tw` would also break).
- **Verify GBP post next Tuesday (2026-07-15)**: Waiting to confirm the fixed routine creates a Teamwork task. Ross: "let's just wait till tomorrow and see if it works."

### New open items
- **Push `~/.claude` to remote**: `878d750` is local-only. Auto Mode blocked the push. Ross can approve in Claude Code or run `git -C ~/.claude push` in Mac Terminal.
- **Tuesday verification (2026-07-15)**: If the GBP routine fires and no Teamwork task appears by end of day, the token env var or script path needs checking.
- **GSC OAuth token is read-only** (webmasters.readonly scope only) — unchanged, browser re-consent needed for writes.
- **GBP Performance API pending Google approval** — unchanged.
- **3 "crawled, not indexed" blog posts** — unchanged, not yet scoped.
